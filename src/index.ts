import { range } from "./range";
import { debounce } from "./debounce";
import { truncate } from "./truncate";

export { range, debounce, truncate };

export default {
  range,
  debounce,
  truncate,
};
